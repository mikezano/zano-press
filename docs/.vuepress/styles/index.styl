//Automatically applied global style files, generated at the ending of the CSS file, have a higher priority than the default style.

html,body{
	height:100%;
}
html{
	background: linear-gradient(#ccc, #ddd);
}